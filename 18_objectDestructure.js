const inforamtion = {
    name : "Piyush Ghate",
    phone : 7841951111,
    college : "Coeta"
}

// extraction of info from inforamtion object 

console.log(inforamtion.name);

const { name } = inforamtion;
const { phone } = inforamtion;
const { college } = inforamtion;

console.log(name);
console.log(phone);
console.log(college);
